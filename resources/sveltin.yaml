paths:
  build: build
  config: config
  content: content
  static: static
  src: src
  params: params
  lib: lib
  routes: routes
  api: api
  themes: themes
pages:
  content: +page.svx
  index: +page.svelte
  indexendpoint: index.ts
  slug: '[slug].svelte'
  slugendpoint: '[slug].json.ts'
api:
  version: v1
  filename: index.ts
theme:
  file: theme.config.js
  components: components
  partials: partials
