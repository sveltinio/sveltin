paths:
  build: build
  config: config
  content: content
  static: static
  src: src
  params: params
  lib: lib
  routes: routes
  api: api
  themes: themes
pages:
  content: index.svx
  index: index.svelte
  indexendpoint: index.ts
  slug: '[slug].svelte'
  slugendpoint: '[slug].ts'
api:
  version: v1
  filename: index.ts
theme:
  file: theme.config.js
  components: components
  partials: partials
