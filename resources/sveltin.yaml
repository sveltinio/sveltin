paths:
  build: build
  config: config
  content: content
  static: static
  src: src
  params: params
  lib: lib
  routes: routes
  api: api
  themes: themes
pages:
  content: index.svx
  index: +page.svelte
  indexendpoint: +page.ts
  slug: +page.svelte
  slugendpoint: +page.ts
  sluglayout: +layout.svelte
api:
  version: v1
  filename: +server.ts
theme:
  file: theme.config.js
  components: components
  partials: partials
