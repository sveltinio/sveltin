paths:
  build: build
  config: config
  content: content
  static: static
  src: src
  params: params
  lib: lib
  routes: routes
  api: api
  themes: themes
pages:
  content: index.svx
  index: +page.svelte
  index_pageload: +page.server.ts
  slug: +page.svelte
  slug_pageload: +page.server.ts
  slug_layout: +layout.svelte
api:
  version: v1
  filename: +server.ts
theme:
  file: theme.config.js
  components: components
  partials: partials
